# TaskBubble

TaskBubble is an interactive web app for a small team (~10 people) to create and manage tasks visualized as floating bubbles.

## Docs
- See `docs/TaskBubble.md` for the product overview, MVP scope, and initial data model.

## Setup (placeholder)
This repo is being bootstrapped. Next steps will include:
- Initialize Git + connect to GitHub
- Create a Supabase project and link this repo (Auth + database)

### Environment variables
Copy `env.example` to `.env` (do not commit `.env`), then fill in your Supabase values.


